---
title: "Tutorial Title"
output:
  pdf_document:
    toc: true
    number_sections: true
    latex_engine: xelatex
    includes:
      in_header: biostatlab-tutorial-pdf.tex
geometry: margin=1in
fontsize: 11pt
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  fig.align = "center"
)

library(biostatlab)
```

This template keeps the same structure as the HTML tutorial but uses a clean PDF layout for sharing and review.

# Overview

Use this section to explain the goal of the tutorial, the dataset, and the main question you want to answer.

# Data and registry

```{r registry}
dataset_registry()
```

```{r available}
available_datasets()
```

# Example workflow

```{r example}
data <- load_dataset("heart_failure")
head(data)
```

# Analysis

Add your steps here: cleaning, exploration, modelling, and interpretation.

# Takeaways

Summarize the main lesson and any next steps for the reader.
